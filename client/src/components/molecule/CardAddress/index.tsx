"use client";

import { FC } from "react";

interface CartAddressProps {
  deliveryAddress?: string; // Adjust based on IDeliveryAddress if necessary
}

const CartAddress: FC<CartAddressProps> = () => {
  return <></>;
};

export default CartAddress;
